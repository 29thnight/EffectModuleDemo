# ImpactBurst — 재사용 가능한 Three.js 이펙트 모듈

```bash
npm install && npm run dev
```

브라우저가 자동으로 열립니다 (Vite 기본 포트 5173). Node 20 이상이면 됩니다.
VS Code는 `EffectModuleDemo.code-workspace`를 열면 됩니다.

**설치 없이 보시려면:** <https://29thnight.github.io/EffectModuleDemo/>
`main`에 푸시될 때마다 GitHub Actions가 `npm run build`(타입 검사 포함)를 돌려 배포합니다.
성능 수치를 보실 거라면 로컬 실행을 권합니다 — 배포본도 같은 코드지만,
측정값은 결국 그걸 돌린 기기의 성능이라 평가자 본인 기기에서 재는 편이 의미가 있습니다.

| 명령 | 하는 일 |
| --- | --- |
| `npm run dev` | 개발 서버 + 브라우저 자동 실행 |
| `npm run build` | `tsc --noEmit` 후 프로덕션 빌드 |
| `npm run typecheck` | 타입 검사만 |
| `npm run bench:cpu` | 브라우저 없이 CPU 적분 비용만 측정 (아래 "성능 측정" 참고) |

---

## 이 과제에서 실제로 풀려고 한 문제

파티클을 예쁘게 그리는 것이 아니라, **씬이 이펙트에 대해 알아야 하는 것을 최소화하는 것**입니다.
그래서 이 README도 "무엇을 만들었나"보다 "무엇을 숨겼고 왜 그렇게 갈랐나"에 지면을 씁니다.

선택이 갈렸던 지점들의 근거와 탈락한 대안은 [docs/DECISIONS.md](docs/DECISIONS.md)에 항목별로 있습니다.

---

## 1. 씬이 이펙트에 대해 아는 것 — 정확히 3가지

`src/core/types.ts`의 `SceneModule`이 계약 전부입니다.

### (1) `scene.add(effects.object3D)` — 두 씬에서 문자 그대로 동일한 한 줄

```ts
// src/scenes/IndoorScene.ts:79   그리고   src/scenes/OutdoorScene.ts:81
attachEffects(effectRoot: Object3D): void {
  this.scene.add(effectRoot)
}
```

인자 타입이 `Points`가 아니라 `Object3D`인 것이 요점입니다.
내부 구현이 `Points`든 인스턴싱 `Mesh`든 `Group`이든 이 줄은 바뀌지 않습니다.

씬 전환 시 하네스가 `remove()`를 부르지 않는다는 점도 봐주세요.
`Object3D`는 부모가 하나뿐이라 새 씬에서 `add()` 한 번이면 이전 씬에서 자동으로 빠집니다.
씬이 알아야 할 것이 하나 줄었습니다.

### (2) "어디서 터지는가"

```ts
getEmitPoint(target: EmitPoint): EmitPoint
```

씬이 좌표와 방향만 채워서 돌려줍니다. 씬 A는 벽·바닥 패널 위의 한 점과 안쪽 법선을,
씬 B는 카메라 진행 방향 앞쪽 지면과 `+Y`를 줍니다.
**언제** 터질지는 씬이 모릅니다 — 하네스의 `EmissionScheduler`가 정합니다.

새 객체를 만들지 않고 하네스가 넘긴 `target`을 채우는 이유는,
초당 수백 개의 `Vector3` 쓰레기가 그대로 p95 프레임 타임에 찍히기 때문입니다.
측정이 산출물인 과제에서는 전역 불변성 규칙보다 무할당이 우선입니다.

### (3) "어떻게 보여야 하는가" — 선언만, 적용은 하네스

```ts
readonly effectStyle: EffectStylePatch   // 선언
```

씬은 `setStyle()`을 부르지 않습니다. 하네스가 씬 활성화 시점에 적용합니다.
씬 B의 선언 한 줄이 이 분리의 존재 이유를 그대로 보여줍니다:

```ts
blending: 'normal',   // 밝은 배경에서 가산 합성은 배경보다 밝아질 수 없어 거의 안 보인다
```

### 검증: 씬의 `update`/`dispose`에 이펙트 코드 0줄

```ts
// IndoorScene — 정지 씬이라 몸통이 비어 있다
update(_dt: number, _elapsed: number): void {}

// OutdoorScene — 카메라 이동만
update(_dt: number, elapsed: number): void {
  this.orbitAngle = elapsed * OUTDOOR_ORBIT_SPEED
  this.placeCamera()
}

// 두 씬 공통 — 자기가 만든 것만 회수
dispose(): void {
  this.disposables.disposeAll()
}
```

씬이 `effects/`에서 가져오는 것은 **타입뿐**입니다. 직접 확인할 수 있습니다:

```bash
grep -n "effects/" src/scenes/*.ts
# 전부 `import type` 입니다. 런타임 심볼은 0개.
```

> 작성 중에 실제로 이게 깨졌었습니다. 씬이 벽 법선으로 평면 기저를 만들 때
> `effects/constants`의 `BASIS_FALLBACK_THRESHOLD`와 `effects/rng`의 `createLcg`를
> 런타임 import 하고 있었습니다. 두 줄 때문에 위 주장이 문자 그대로는 거짓이 되므로,
> 공용 수학을 `src/shared/`로 옮겼습니다.

---

## 2. 공개 API는 두 층 — `src/effects/types.ts`

### 층 1 · 생성 시 고정 (`ImpactBurstConfig`)

바꾸면 GPU 버퍼를 다시 잡아야 하는 값들입니다.
setter를 두면 프레임 중간에 재할당이 숨어버리므로 **타입으로 잠갔습니다**.

| 항목 | 왜 여기 있나 |
| --- | --- |
| `capacity` | 슬롯 총량. 바꾸면 `Float32Array` 6개와 GPU 버퍼 3개를 새로 잡아야 한다 |
| `particlesPerBurst` | 링 할당 단위. 바꾸면 진행 중인 링의 슬롯 정렬이 깨진다 |
| `overflow` | 정책마다 슬롯 회수 경로가 달라 런타임 전환은 거짓말이 된다 |
| `seed` | 중간에 바꾸면 측정 재현성이 사라진다 |

디버그 패널에서 오버플로 정책을 바꿔보면 로그에 이렇게 남습니다:

```
오버플로 정책을 'drop-burst'로 바꾸며 이펙트 인스턴스를 재생성했습니다.
```

재생성이 일어난다는 사실을 감추지 않는 것이 이 API의 요점입니다.

### 층 2 · 런타임 변경 (`EffectStyle`)

색 2종 · 크기 2종 · 수명 · 수명 편차 · 속력 · 속력 편차 · 분사각 · 중력 · 감쇠 · 알파 · 합성 방식.

13개 항목이 전부 **유니폼 또는 머티리얼 상태**(`src/effects/burstMaterial.ts`)
아니면 **CPU 적분 계수**로 흡수되고, 버퍼를 건드리는 항목이 하나도 없다는 것이
"런타임 변경 가능"이라는 주장의 실제 근거입니다.

### 절대 공개하지 않는 것

`ShaderMaterial` · `BufferGeometry` · `BufferAttribute` · `Float32Array` ·
링 커서(`head` / `span`) · `THREE.Points` 인스턴스.

전부 CPU 적분을 GPU 적분으로 갈아끼울 때 사라지거나 형태가 바뀌는 것들입니다.
공개하는 순간 교체가 파괴적 변경이 됩니다.
`src/effects/ParticleBuffers.ts`를 따로 뺀 이유도 같습니다 —
GPU 적분으로 옮기면 `velocities`/`ages`/`lifespans`가 사라지고 부동소수점 텍스처 두 장이 들어오는데,
**바뀔 파일과 안 바뀔 파일이 갈려 있어야** 그 교체가 실제로 가능합니다.

---

## 3. 자원 소유권

| 주체 | 소유 | 회수 |
| --- | --- | --- |
| `ImpactBurst` | geometry, material, 6개 `Float32Array` | `dispose()`에서 전부. 부모에서 스스로 빠지는 것 포함 |
| 씬 | 자기가 만든 geometry / material | `Disposables`에 등록한 것만 |
| `App`(하네스) | 이펙트 **인스턴스**, renderer | 앱 종료 시 |

씬은 이펙트 자원을 **절대** 해제하지 않습니다.
`scene.traverse()`로 순회하며 지우는 흔한 방식을 쓰지 않은 이유가 정확히 이것입니다 —
그 순간 이펙트 루트도 씬 그래프에 붙어 있고, 이펙트는 씬 것이 아닙니다.
그래서 `src/scenes/Disposables.ts`로 "내가 등록한 것만 내가 지운다"로 뒤집었습니다.

### 인스턴스를 하네스가 갖는 대가

씬마다 이펙트를 두면 전환 때 5,120,000 슬롯짜리(약 205MB) 버퍼가 새로 할당됩니다.
그래서 하네스가 하나만 갖고 두 씬이 공유합니다. **씬 전환 시 재할당 0회.**

대가는 **전환 시점에 살아있던 파티클이 새 씬으로 넘어온다**는 것입니다.
버그가 아니라 위 선택의 청구서이고, 최대 수명(1.4초) 안에 자연 소멸합니다.
유니폼은 즉시 교체되므로 이월 파티클도 새 씬 색으로 바뀌어 전환 순간 색이 한 번 튑니다.
(파티클별 색 attribute를 두면 옛 색을 유지할 수 있지만, 합성 방식은 머티리얼 단위라
어차피 분리할 수 없어 절반만 해결하면서 파티클당 3 float를 더 씁니다. 그래서 택하지 않았습니다.)

---

## 4. 파일 구조

```
src/
  effects/          이펙트 모듈. 씬은 types.ts만 안다
    types.ts          공개 계약 — 씬이 알아야 할 전부
    ImpactBurst.ts    링 커서 + CPU 적분 + 스폰
    ParticleBuffers.ts  버퍼 소유와 부분 업로드 (GPU 적분 전환 시 바뀔 파일)
    burstMaterial.ts  유니폼과 머티리얼 (런타임 스타일이 흘러 들어가는 곳)
    shaders.ts        GLSL. 외부 텍스처 없음
    constants.ts      모든 수치와 그 근거
  core/             하네스
    types.ts          SceneModule 계약 + 씬별 렌더러 프로필 + UI용 관측 타입
    App.ts            렌더 루프 · 씬 전환 · 리사이즈 · 이펙트 소유 · dt 클램프
    EmissionScheduler.ts  "동시 N개"의 정의를 코드로 못박은 곳
    renderer.ts       렌더러 생성 · 프로필 적용 · DPR 해석
    Telemetry.ts      관측값을 UI 형태로 옮기는 곳
    probes.ts         해제된 모듈 두들기기
  scenes/           A: 어두운 실내/정지   B: 밝은 야외/이동
  bench/            FrameSampler(평균 + p95) · LoadSweep · cpuBench(헤드리스)
  ui/               DebugPanel. three import 0건
  shared/           rng(고정 시드 LCG) · basis(직교 기저)
```

의존 방향은 `ui → core → effects → shared` 한쪽뿐입니다. 확인:

```bash
grep -rn "from 'three'" src/ui/          # 0건
grep -rn "from '.*ui/" src/core src/effects src/scenes src/bench   # 0건
```

---

## 5. 성능 측정

### 먼저 "동시 N개"를 정의합니다

과제 지문의 "수명 L초 버스트를 초당 N/L회"는 버스트 1회가 파티클 1개일 때만 성립합니다.
버스트가 P개를 뱉으면 정상 상태 생존 수가 `N × P`가 되어 목표를 P배 초과합니다.
그래서 **N을 "동시에 살아있는 파티클 수"로 정의**하고 역산합니다:

```
정상 상태에서 초당 생성 = 초당 소멸
alive = burstsPerSecond × P × L
→ burstsPerSecond = N / (L × P)          (P = 1이면 지문의 N/L과 동일)
```

`src/core/EmissionScheduler.ts`에 이 식 그대로 들어 있습니다.
정의가 맞는지는 결과의 **실측 생존** 열이 증명합니다 — 목표 32,000에 대해 31,696 (99.0%).

### 방법

- 단계마다 **워밍업 1.5초 후 3초 측정**. 워밍업은 정상 상태 도달(수명 1회분)과
  셰이더 컴파일·첫 버퍼 업로드를 측정에서 빼기 위한 것입니다.
- 부하는 2배씩 증가. 로그 축에서 기울기(비용이 선형인지 제곱인지)를 바로 읽기 위해서입니다.
- 난수는 고정 시드 LCG. 두 번 돌렸을 때 파티클 분포가 달라지면
  프레임 차이가 "코드 때문"인지 "이번엔 파티클이 카메라에 더 가까웠기 때문"인지 구분할 수 없습니다.
- **평균 FPS만 기록하지 않습니다.** 세 계열을 따로 잽니다:

| 계열 | 무엇 | 읽는 법 |
| --- | --- | --- |
| 프레임 | rAF 콜백 간격 (vsync·GPU 포함) | 화면에 실제로 나온 프레임 타임 |
| CPU 프레임 | 우리 JS가 돈 시간 | `프레임 ≫ CPU` → GPU 병목 |
| 이펙트 CPU | 그중 파티클 적분만 | `CPU ≈ 이펙트` → GPU 적분 전환의 근거 |

이 구분이 없으면 "느리다"까지만 알고 "다음에 뭘 고칠까"는 못 고릅니다.

### 결과 A — 헤드리스 CPU 적분 (`npm run bench:cpu`)

GPU·버퍼 업로드·드로우 콜을 전부 뺀 순수 CPU 비용입니다. 고정 dt 16.67ms.
용량 5,120,000 / 버스트당 24개 / `recycle-oldest` / L = 0.9s.

| 목표 N | 실측 생존 | draw 슬롯 | 이펙트 CPU avg | p95 | max | 재활용 | 드롭 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 500 | 495 | 650 | 0.011 ms | 0.014 ms | 0.106 ms | 0 | 0 |
| 1,000 | 987 | 1,311 | 0.014 ms | 0.017 ms | 0.037 ms | 0 | 0 |
| 2,000 | 1,973 | 2,638 | 0.030 ms | 0.048 ms | 0.164 ms | 0 | 0 |
| 4,000 | 3,955 | 5,297 | 0.053 ms | 0.057 ms | 0.188 ms | 0 | 0 |
| 8,000 | 7,927 | 10,625 | 0.106 ms | 0.132 ms | 0.203 ms | 0 | 0 |
| 16,000 | 15,839 | 21,293 | 0.213 ms | 0.295 ms | 0.456 ms | 0 | 0 |
| 32,000 | 31,696 | 42,619 | 0.432 ms | 0.581 ms | 0.767 ms | 0 | 0 |
| 64,000 | 63,405 | 85,289 | 0.846 ms | 0.933 ms | 1.253 ms | 0 | 0 |
| 128,000 | 126,823 | 170,627 | 1.710 ms | 1.822 ms | 2.854 ms | 0 | 0 |
| 256,000 | 253,598 | 341,290 | 3.478 ms | 3.731 ms | 5.310 ms | 0 | 0 |
| 512,000 | 507,277 | 682,626 | 7.081 ms | 7.723 ms | 8.437 ms | 0 | 0 |
| 1,024,000 | 1,014,518 | 1,365,285 | 13.920 ms | 14.591 ms | 19.937 ms | 0 | 0 |
| 2,048,000 | 2,029,079 | 3,673,094 | 29.235 ms | 30.675 ms | 31.785 ms | 0 | 0 |

읽을 점 세 가지:

1. **비용은 생존 수에 선형입니다.** 500 → 2,048,000까지 4,096배 부하에 2,658배 시간.
   기울기로 환산하면 파티클 1,000개당 약 14.4us입니다. 이 선형성이 아래 2번을 예측 가능하게 만듭니다 —
   상한을 어디에 둘지 추측하지 않고 계산해서 정할 수 있습니다.
2. **CPU만으로 60fps가 깨지는 지점이 있습니다.** 1,024,000에서 13.920ms(예산 16.67ms의 83%),
   2,048,000에서 29.235ms(175%)입니다. **GPU가 아무리 빨라도** 이 구간에서는 프레임이 밀립니다.
   상한을 2,048,000으로 잡은 근거가 이것입니다 — 여기까지 올려야 "한계를 못 찾았다"로 끝나지 않습니다.
   동시에 이것이 GPU 적분(TransformFeedback / GPGPU 텍스처) 전환의 첫 실측 근거이기도 합니다.
   32,000에서 0.43ms일 때는 옮길 이유가 없었지만, 100만 개에서 13.9ms면 이야기가 다릅니다.
3. **실측 생존과 draw 슬롯 열은 재실행해도 한 자리까지 동일합니다.** 고정 시드 LCG + 고정 dt의 결과이고,
   이 재현성이 없으면 위 1번 결론을 믿을 수 없습니다.

### 결과 B — 브라우저 스윕 (패널의 "부하 스윕 실행")

측정 환경: **NVIDIA GeForce RTX 2080 Ti** (ANGLE / D3D11) · Microsoft Edge 152 · Windows
씬 A · 뷰포트 1905 × 1890 px @ DPR 1.5 · 용량 5,120,000 / 버스트당 24개 / `recycle-oldest` / L = 0.9s

| 목표 N | 실측 생존 | draw 슬롯 | bursts/s | FPS | 프레임 avg | 프레임 p95 | CPU 프레임 avg | CPU 프레임 p95 | 이펙트 CPU avg | 이펙트 CPU p95 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 0 | 0 | 0 | 0.0 | 60.0 | 16.67 | 16.80 | 0.20 | 0.30 | 0.01 | 0.10 |
| 500 | 494 | 675 | 23.1 | 60.0 | 16.67 | 16.80 | 0.25 | 0.40 | 0.03 | 0.10 |
| 1,000 | 986 | 1,359 | 46.3 | 60.0 | 16.67 | 16.80 | 0.23 | 0.30 | 0.04 | 0.10 |
| 2,000 | 1,981 | 2,737 | 92.6 | 60.0 | 16.67 | 16.80 | 0.27 | 0.40 | 0.07 | 0.20 |
| 4,000 | 3,963 | 5,501 | 185.2 | 60.0 | 16.67 | 16.80 | 0.36 | 0.50 | 0.15 | 0.20 |
| 8,000 | 7,916 | 11,038 | 370.4 | 60.0 | 16.67 | 16.80 | 0.52 | 0.70 | 0.27 | 0.40 |
| 16,000 | 15,873 | 22,149 | 740.7 | 60.0 | 16.67 | 16.80 | 0.92 | 1.20 | 0.58 | 0.70 |
| 32,000 | 31,699 | 44,368 | 1481.5 | 60.0 | 16.67 | 16.80 | 1.61 | 1.90 | 1.10 | 1.30 |
| 64,000 | 63,436 | 88,813 | 2963.0 | 60.0 | 16.67 | 16.80 | 2.67 | 3.20 | 1.95 | 2.40 |
| 128,000 | 126,721 | 177,703 | 5925.9 | 60.0 | 16.67 | 16.80 | 4.12 | 4.80 | 3.22 | 3.80 |
| 256,000 | 253,708 | 355,474 | 11851.9 | 60.0 | 16.67 | 16.80 | 6.28 | 6.70 | 4.96 | 5.40 |
| 512,000 | 507,255 | 808,458 | 23703.7 | 60.0 | 16.67 | 16.80 | 12.11 | 12.90 | 9.54 | 10.20 |
| **1,024,000** | 1,006,861 | 1,483,962 | 47407.4 | **35.8** | **27.93** | **33.40** | 27.54 | 29.80 | 19.17 | 20.10 |
| **2,048,000** | 1,968,490 | 3,811,126 | 94814.8 | **14.2** | **70.54** | **83.40** | 69.96 | 75.70 | 40.27 | 42.10 |

드롭·재활용은 전 구간 0입니다(용량 5,120,000 안에서 끝났습니다). 도구가 자동으로 붙인 해석:

> - 최대 부하(동시 2,048,000개)에서 프레임 avg 70.54ms / p95 83.40ms
> - 그중 우리 JS가 69.96ms, 이펙트 CPU 적분이 40.27ms (CPU 시간의 58%)
> - 프레임 − CPU 간극 0.58ms
> - 병목 판정: **이펙트 CPU 적분.** 이 시점부터 GPU 적분(TransformFeedback / GPGPU 텍스처)
>   전환이 근거를 갖는다.

**무릎은 1,024,000과 512,000 사이에 있습니다.** 512,000까지는 프레임 p95가 16.80ms로 vsync에 붙어
있다가, 1,024,000에서 27.93ms로 무너집니다. 60fps를 유지하는 최대 부하가 **약 50만 개**라는 뜻입니다.

**그리고 병목은 GPU가 아니라 CPU입니다.** 최대 부하에서 프레임 − CPU 간극이 **0.58ms**입니다 —
프레임 70.54ms 중 69.96ms를 JS가 쓰고, RTX 2080 Ti는 사실상 놀고 있습니다.
파티클 200만 개를 그리는 것보다 **200만 개를 CPU에서 적분하는 것이 100배 이상 비쌉니다.**

이 결과가 나오기까지 상한을 두 번 올렸습니다. 처음 상한은 32,000이었고 판정은
`vsync 고정 — 이 부하 범위에서는 한계에 도달하지 못했다`였습니다. 256,000까지 올려도 같았습니다.
"한계를 못 찾았다"와 "GPU 병목"을 구분하는 판정 로직이 없었다면, 두 번 다
"32,000개에서 60fps 나오니 충분"으로 끝났을 것입니다.

이 구분이 처음에는 잡히지 않았습니다. 초기 판정 로직은 "프레임 − CPU 간극이 크면 GPU 병목"이라고만
말했고, 이 결과에 대고 `GPU(프래그먼트 오버드로우) 또는 vsync 대기`라는 애매한 답을 냈습니다.
그런데 둘은 전혀 다른 상황입니다 — 하나는 "더 못 올린다"이고 다른 하나는 "아직 안 밀어봤다"입니다.
그래서 판정에 vsync 고정 검사를 넣었습니다(`프레임 p95 ≤ avg × 1.05` 이면서 `CPU < 프레임 × 0.4`).
지금 출력이 그 결과입니다.

읽을 점:

- **CPU 쪽은 부하에 선형입니다.** 이펙트 CPU 적분 0.01 → 40.27ms (N 0 → 2,048,000).
  헤드리스 벤치의 기울기(1,000개당 14.4us)와 같은 직선 위에 있고, 브라우저 쪽이 약간 더 비싼 것은
  버퍼 업로드가 포함되기 때문입니다.
- **`draw 슬롯` 열은 여전히 조심해서 보세요.** 이번 실행에서는 헤드리스 값과 잘 맞습니다
  (N=8,000에서 11,038 vs 10,625, N=2,048,000에서 3,811,126 vs 3,673,094). 다만 이전 실행에서는
  N=8,000이 27,091로 튄 적이 있습니다. 링 버퍼가 배열 끝을 걸치는 구간이 측정 창(3초)보다 긴 주기로
  지나가기 때문이고, 그 구간이 창에 들어가느냐는 운입니다. 아래 "알려진 한계"에 자세히 적었습니다.
- **씬 B는 더 무겁습니다.** 밝은 배경 + 일반 합성이라 프래그먼트 비용이 큽니다.
  DPR 상한도 1.5로 다르므로, 씬 간 비교는 패널에서 DPR을 같은 값으로 고정한 뒤에 하세요.


### 직접 재현하는 법

1. `npm run dev`
2. 좌측 패널에서 씬을 고릅니다 (씬 간 비교 시 **DPR을 같은 값으로 고정**하세요 —
   씬 A는 프로필 상한 2, 씬 B는 1.5로 서로 다릅니다).
3. **부하 스윕 실행** → 약 2분 (14단계 × 4.5초. 상위 단계는 프레임이 길어져 실제 소요는 더 깁니다)
4. **결과 복사** → 마크다운 표가 클립보드로. 그대로 붙여넣으면 됩니다.
5. CPU만 따로: `npm run bench:cpu`

---

## 6. 비정상 상황 — 설계된 동작과 실측

전부 패널의 "비정상 상황 검증"에서 직접 재현할 수 있습니다.

> 실측 관찰 열은 상한을 올리기 전(용량 60,000 / 풀 고갈 목표 120,000) 설정의 기록입니다.
> 현재 「풀 고갈」 버튼은 용량 5,120,000에 대해 목표 10,240,000을 겁니다 — 비율은 같으므로
> 설계된 동작은 그대로지만, 재활용 건수 같은 절대 수치는 다시 재야 합니다.

| 상황 | 재현 | 설계된 동작 | 실측 관찰 |
| --- | --- | --- | --- |
| **풀 고갈** | 「풀 고갈」 버튼 (목표 10,240,000 vs 용량 5,120,000) | `recycle-oldest`: 가장 오래된 슬롯을 O(1)로 회수. 새 버스트는 항상 보인다 | 생존 56,695 / draw 슬롯 60,000 / **재활용 407,903건, 드롭 0**. 드로우 콜은 여전히 1 |
| 〃 (드롭 정책) | 오버플로 선택을 `drop-burst`로 | 살아있는 파티클은 온전히 수명을 다하고 새 버스트가 통째로 무시된다 | **드롭 6,896건, 재활용 0**. 정책 변경 시 인스턴스 재생성 로그가 남는다 |
| **탭 복귀 dt 폭주** | 「dt 폭주 주입」 (5초 주입) 또는 실제로 탭 전환 후 복귀 | `dt = min(dt, 0.1s)`. 파티클이 한 프레임에 수명을 소진하거나 화면 밖으로 날아가지 않는다 | **raw 5066.8ms → clamp 100.0ms**. 로그에 감지 메시지가 남고, 파티클 상태는 그대로 유지됨 |
| **씬 전환 중 이펙트 생존** | 씬 A ↔ B 라디오 | 인스턴스는 유지, 버퍼 재할당 0회. 살아있던 파티클은 새 씬으로 이월되어 최대 수명 안에 소멸. 유니폼 즉시 교체로 색이 한 번 튄다 | 전환 즉시 `bursts/s`가 92.6 → 59.5로 바뀜 (하네스가 새 씬의 선언된 수명 L=1.4s에서 역산). 이월 파티클이 새 씬 색으로 계속 날아감 |
| **dispose 후 호출** | 「dispose 후 호출」 버튼 | 모든 공개 API가 조용히 무시된다. 예외 없음, 상태 변화 없음, 드로우 콜 0. `dispose()` 중복 호출도 안전 | **예외 없음 / disposed=true / alive 1904 → 0 / drawCalls 0 / burstsRequested 증가 없음**. 이후 새 인스턴스로 교체해 데모 계속 사용 가능 |
| **절두체 밖 발생** | 「절두체 밖에서 발사」 체크 (카메라 뒤 40m) | 슬롯과 CPU 적분 비용은 정상 소비. 정점 셰이더까지 돌지만 프래그먼트는 0. `frustumCulled = false`라 컬링으로 사라지지도 않는다 | 화면에 파티클이 하나도 안 보이는데 생존 수와 이펙트 CPU는 그대로. 프레임 타임만 내려감 |
| **DPR 변경** | 패널의 DPR 셀렉트, 또는 창을 다른 배율 모니터로 이동 | 매 프레임 `devicePixelRatio`를 비교해 바뀌면 리사이즈. 드로잉 버퍼 크기를 이펙트에 다시 먹여 파티클 픽셀 크기를 맞춘다 | DPR 1 / 1.5 / 2 전환 시 파티클 크기가 화면상 동일하게 유지됨. 하드웨어 상한 2로 클램프 |

### 「부하·관측 리셋」이 되돌리는 것과 되돌리지 않는 것

프로브를 돌린 뒤 이 버튼을 누르면 무엇이 실제로 복귀하는지 헷갈릴 수 있어 분리해 적습니다.
아래는 프레임을 한 장도 그리지 않은 상태에서 버튼만 눌러 측정한 값입니다.
(옛 설정의 기록이라 좌변은 풀 고갈 목표 120,000 기준입니다. 현재는 10,240,000 → 2,000이 됩니다.)

| 항목 | 리셋 후 | 성격 |
| --- | --- | --- |
| 목표 N | 120,000 → **2,000** | 즉시 복귀 |
| bursts/s | 5,555.6 → **92.6** | 즉시 복귀 |
| dt 관측최대 | 6,016.5ms → **0.0ms** | 즉시 복귀 (래치 해제) |
| 프레임 표본(avg/p95) | **초기화** | 즉시 복귀 |
| 살아있는 파티클 | 1,377개 그대로 | **복귀 대상 아님** — 이미 발사된 것은 수명(최대 1.26초)을 다 산다 |
| 누적 드롭 / 재활용 | 그대로 | **복귀 대상 아님** — 인스턴스 수명 전체의 누적 카운터 |

즉 리셋 직후 화면이 몇 초간 여전히 폭발적으로 보이는 것은 정상입니다.
제어값(목표·발사 속도)은 이미 돌아왔고, 눈에 보이는 것은 잔여물입니다.
「생존 / 목표 N」 행에서 이 둘이 분리되어 보입니다.

`frustumCulled = false`인 이유: 파티클은 월드 좌표로 매 프레임 흩어지므로
바운딩 스피어를 다시 구하려면 `O(capacity)` 순회가 필요합니다.
컬링을 끄는 편이 그보다 쌉니다.

---

## 7. 검증

| 항목 | 결과 |
| --- | --- |
| `tsc --noEmit` | 통과 (strict + `noUnusedLocals` + `noUnusedParameters` + `verbatimModuleSyntax`) |
| `vite build` | 통과 (1.8초) |
| 브라우저 실제 구동 | 씬 A·B 모두 렌더링 및 이펙트 발생 확인 (Edge 152 / RTX 2080 Ti) |
| 부하 스윕 | 14단계 완주. 512,000까지 60.0 FPS, 1,024,000에서 35.8 FPS로 무릎 확인 |
| 콘솔 에러 | **0건** (Vite HMR 디버그 로그만) |
| 파일 크기 | 최대 395줄 (`core/App.ts`). 전부 400줄 이내 |
| 함수 크기 | 38줄 이상 함수 0개 |
| 이펙트 드로우 콜 | 씬 A에서 전체 7회 중 **1회**, 씬 B에서 전체 22\~33회 중 **1회** |
| 계기 즉시성 | 프레임 0개 상태에서 버튼만 눌러도 패널이 실제 상태를 반영 (아래 §8 참고) |

---

## 8. 알려진 한계와 다음 수순

**지금 상태의 한계**

- **용량 5,120,000이 기동 시 약 205MB를 잡습니다.** (위치 3 + 수명 1 + 시드 1) × 4바이트 ×
  5,120,000 = 102.4MB GPU 버퍼, CPU 전용 속도·나이·수명이 또 102.4MB입니다.
  기본 부하(2,000)로만 쓰는 사람에게는 순수 낭비입니다 — 이 데모의 목적이 과부하 측정이라
  감수한 비용이고, **모듈을 가져다 쓰는 쪽은 `capacity`를 자기 상한에 맞춰 낮춰야 합니다.**
  `ImpactBurstConfig.capacity`는 생성 시 고정 옵션이라 소비자가 직접 정할 수 있습니다.
- **부하가 너무 크면 목표 N에 도달하지 못합니다.** 프레임이 dt 클램프(0.1s)보다 길어지면
  시뮬레이션이 프레임당 0.1초씩만 진행되어 측정 구간 안에 정상 상태에 못 이릅니다.
  이때 기록되는 프레임 타임은 목표 N이 아니라 실제 생존 수의 성능이므로 다른 단계와 비교할 수 없습니다.
  스윕 판정이 `TARGET_REACHED_RATIO`(0.8) 미만인 단계를 「목표 미달」로 먼저 걸러내지만,
  그 단계 자체는 여전히 버려지는 측정입니다.
- **draw 슬롯 열이 브라우저 스윕에서 노이즈가 큽니다.** 링이 배열 끝을 걸치는 프레임에는
  `drawRange`를 두 조각으로 나눌 수 없어 전체 용량을 그립니다. 그런데 링이 한 바퀴 도는 주기가
  높은 부하에서 측정 창(3초)보다 길어서, "측정 구간에 감긴 프레임이 얼마나 들어갔나"가 운에 좌우됩니다.
  이전 실행이 그대로 보여줬습니다 — N=4,000에서 5,502인데 N=8,000에서 27,091이고,
  같은 조건을 결정적으로 도는 헤드리스 벤치에서는 10,625였습니다.
  (위 최신 표에서는 우연히 재현되지 않았습니다. 사라진 것이 아니라 운이 좋았던 것입니다.)
  헤드리스 쪽은 단계마다 인스턴스를 새로 만들어 링 위치가 고정이라 이 문제가 없습니다.
  고치려면 측정 창을 링 주기보다 길게 잡거나, 감긴 프레임만 드로우 콜 2회를 허용하면 됩니다.
- **브라우저의 `performance.now()` 해상도가 0.1ms로 뭉개져 있습니다**(Spectre 완화).
  그래서 브라우저 스윕의 이펙트 CPU 열은 낮은 부하에서 0.01\~0.10ms로 계단이 집니다.
  헤드리스 벤치가 0.001ms 단위로 나오는 이유입니다.
- **이펙트가 씬 fog를 따르지 않습니다.** 셰이더에 fog 청크를 넣지 않았습니다.
  지금은 두 씬 다 fog가 없어 문제가 없지만, 넣는 순간 파티클만 안개를 무시합니다.
- **파티클끼리, 그리고 파티클과 지오메트리 간 정렬이 없습니다.** `depthWrite: false`라
  서로 잘라먹지는 않지만 겹침 순서는 링 순서를 따릅니다. 가산 합성에서는 순서가 무관하고,
  일반 합성(씬 B)에서는 이론상 보이지만 파티클이 반투명하고 작아 눈에 띄지 않습니다.
- 번들이 546 kB(gzip 141 kB)입니다. 대부분 three이고, 코드 스플리팅은
  "`npm install` 1회 + `npm run dev` 1회"라는 하드 제약에 아무것도 보태지 않아 하지 않았습니다.

**다음에 손댈 순서와 그 근거**

0. **상한을 찾았습니다.** RTX 2080 Ti에서 60fps 상한은 **약 50만 개**이고, 그 위의 병목은
   **GPU가 아니라 CPU 적분**입니다(최대 부하에서 프레임 − CPU 간극 0.58ms). 이제 아래 판단들이
   추측이 아니라 측정 위에 섭니다.
1. **일반적인 사용 범위에서는 아무것도 안 함.** 실제 게임이 동시에 띄우는 파티클은 수천\~수만 개
   수준이고, 그 구간의 이펙트 CPU 적분은 0.03\~1.10ms(예산의 0.2\~6.6%)입니다.
   여기서 최적화할 곳은 이펙트가 아닙니다. 아래 2번부터는 **동시 50만 개를 실제로 요구할 때만**
   근거가 있습니다.
2. **GPU 적분(TransformFeedback / GPGPU 텍스처)이 이제 1순위입니다.** 순서가 바뀐 이유가 측정입니다.
   상한을 찾기 전에는 "CPU 적분이 예산의 30%를 넘으면"이라는 조건부 항목이었는데,
   1,024,000에서 19.17ms(115%), 2,048,000에서 40.27ms(242%)로 그 조건이 충족됐습니다.
   기울기(1,000개당 14.4us)로 역산하면 **동시 약 35만 개**에서 30% 선에 닿습니다.
   그때 바뀌는 파일은 `ParticleBuffers.ts`와 `ImpactBurst.integrateRun()`이고,
   `types.ts`의 공개 계약과 씬 코드는 한 줄도 바뀌지 않습니다. 그러라고 이렇게 갈랐습니다.
3. **프래그먼트는 그 다음입니다.** 최대 부하에서 프레임 − CPU 간극이 0.58ms라는 것은
   RTX 2080 Ti가 파티클 200만 개를 그리는 데 사실상 비용을 안 쓰고 있다는 뜻입니다.
   여기를 먼저 만지면 아무것도 못 법니다. 단 이 결론은 **이 뷰포트(1905 × 1890)와 이 GPU**에
   묶여 있습니다 — 4K 풀스크린이나 내장 GPU에서는 순서가 뒤집힐 수 있으니 거기서 다시 재야 합니다.
4. **정점 비용은 마지막입니다** — 위의 "감긴 프레임만 드로우 콜 2회"입니다.
   draw 슬롯이 생존 수의 1.35\~1.81배이므로 최대 25% 정도의 정점 셰이더 호출을 줄일 수 있는데,
   지금 병목이 CPU인 이상 프레임 타임에는 거의 나타나지 않습니다.

---

## 결정 기록

선택이 갈렸던 지점 전부 — 무엇을 골랐고, 왜 골랐고, 무엇을 왜 버렸는지:
**[docs/DECISIONS.md](docs/DECISIONS.md)**
